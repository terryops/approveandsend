# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# testing
/coverage

# secrets — never commit anything env-shaped
.env
.env.*
!.env.example

# data — holds live customer email once running. Ignore the whole directory
# rather than chase file extensions; the predecessor project ignored
# /data/*.db and still nearly published 23 MB tasks.db.bak-* snapshots.
/data/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
